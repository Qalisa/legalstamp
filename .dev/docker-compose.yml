services:
  web:
    image: legalstamp
    build:
      context: ..
      args:
        K8S_APP__VERSION: "0.dev-compose"
        CANONICAL_URL: http://localhost
        AUTH_BEARER_TOKEN: testtest
      dockerfile: Dockerfile
      tags:
        - latest
    ports:
      - "4321:80"
