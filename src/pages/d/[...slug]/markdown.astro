---
import { availableFormatsConfig, formatStubs } from "helpers/legalstamp.groupedBy";

export const prerender = false

const { slug } = Astro.params;

//
const goTo = [formatStubs.dynamic, slug, availableFormatsConfig.markdown.name].filter(Boolean).join('/')
return Astro.rewrite(goTo)
---
