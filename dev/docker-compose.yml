services:
  web:
    image: legalstamp
    build:
      context: ..
      args:
        K8S_APP__VERSION: "0.dev-compose"
        CORS_ALLOW_ORIGIN: "*"
        CANONICAL_URL: http://localhost
      dockerfile: Dockerfile
      tags:
        - latest
    ports:
      - "4321:80"
